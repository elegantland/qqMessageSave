// 插件加载时触发
exports.onLoad = () => {
    console.log("[Message Save] 1.0.1 loaded");
};

// 创建窗口时触发
exports.onBrowserWindowCreated = (window) => {
};

// 用户登录时触发
exports.onLogin = (uid) => {
};
